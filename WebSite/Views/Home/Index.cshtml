@model ProjectHermes.Models.HomeModel

<section class="HeaderSection">
	<header>
		<h2>Home Page</h2>
	</header>
	<article>
		<div class="subTitle">
			<p class="bigText">This will be a subtitle </p>
		</div>

		<p>The concept of data on this site is simple there is a parent item with child item.</p>
	</article>
</section>
<br style="clear:both" />

   
@using (Html.BeginForm("Index","Home", FormMethod.Post))
{

    @Html.ValidationSummary(true)
    
 <section class="ArticleSection">
	
	@for (var i = 0; i < Model.Organisations.Count(); i++)
    {

     
       <article class="standardarticle" >    
				 <header>
					 @Model.Organisations[i].Name
				 </header>
                <div class="content">
					@Model.Organisations[i].Description

                    @if (Model.Organisations[i].ChildItems.Count() > 0) { 
                    
                    <ul>
                    @foreach (var item in @Model.Organisations[i].ChildItems)
                    {
                      <li> @item.Name </li>
                    }
                    </ul>
                    }
                        


   
                </div>
				 <footer>
					@Html.ActionLink("View Item","Details", new { ID = Model.Organisations[0].Organisationid}, null)
				 </footer>
       </article> 
       
    }
</section>
        
    
}


